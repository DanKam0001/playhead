-r requirements.txt

# --- local tooling: indexing, probes, the original desktop client ---
assemblyai==1.3.0
uvicorn[standard]==0.37.0
sounddevice==0.5.1      # mic capture + playback (PortAudio)
soundfile==0.12.1       # decode wav/flac/ogg
elevenlabs==2.16.0
pytest==9.1.1         # the test suite; needs no keys or hardware
